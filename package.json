{
  "name": "govuk-rewrite-workspace",
  "private": true,
  "workspaces": [
    "packages/core",
    "packages/cli"
  ],
  "scripts": {
    "build": "npm run build -w govuk-rewrite-core && npm run build -w govuk-rewrite",
    "dev": "npm run build -w govuk-rewrite-core && npm run dev -w govuk-rewrite --",
    "test": "npm run test -w govuk-rewrite-core && npm run test -w govuk-rewrite",
    "test:watch": "npm run test:watch -w govuk-rewrite-core"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
