{
  "name": "govuk-rewrite",
  "version": "0.1.0",
  "description": "A tiny CLI that rewrites text into GOV.UK-style content",
  "type": "module",
  "bin": {
    "govuk-rewrite": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "keywords": [
    "govuk",
    "content",
    "rewrite",
    "cli"
  ],
  "license": "MIT",
  "dependencies": {
    "commander": "^12.1.0",
    "govuk-rewrite-core": "0.1.0",
    "ora": "^8.1.1"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/index.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "engines": {
    "node": ">=18"
  }
}
